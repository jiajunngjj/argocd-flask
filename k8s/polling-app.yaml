---
apiVersion: v1
kind: Service
metadata:
  name: polling-app:0.1.5
spec:
  ports:
  - name: 8080-tcp
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: polling-app:0.1.5
  type: LoadBalancer


---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: polling-app:0.1.5
spec:
  replicas: 1
  selector:
   matchLabels:
    app: polling-app:0.1.5
  template:
    metadata:
      labels:
        app: polling-app:0.1.5
    spec:
      containers:
      - image: jiajunngjj/polling-app:0.1.5
        name: polling-app:0.1.5
        ports:
        - containerPort: 8080
      restartPolicy: "Always"
